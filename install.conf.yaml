- defaults:
    clean:
      force: true
    link:
      create: true
      relink: true
      force: true

- clean: ['~', '~/.config']

- shell:
  - [git submodule update --init --recursive, Running git submodule update]

- link:
    ~/.dotfiles: .
